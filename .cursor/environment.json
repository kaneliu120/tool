{
  "name": "REA Kasada HTML Unblocker Toolkit",
  "install": "./.cursor/install.sh",
  "start": "./.cursor/start.sh",
  "terminals": [
    {
      "name": "gateway (mock provider)",
      "command": "./.cursor/start.sh --attach",
      "description": "FastAPI HTML provider gateway on :8080 with the fixture (mock) provider enabled for local integration. POST /v1/fetch-html, GET /healthz. start.sh is idempotent and tails logs in this terminal."
    }
  ],
  "ports": [
    {
      "name": "gateway",
      "port": 8080
    }
  ]
}
